import Logo from "./Logo";
import Menu from "./Menu";

const Header = () => {
    return (
        <div className="header-wrapper">
            <Logo />
            <Menu />
        </div>
    );
};

export default Header;
